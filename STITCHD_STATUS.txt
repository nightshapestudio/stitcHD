====================================================
LATEST MILESTONE:
SOURCE PLAYBACK + SNAP TO GRID
====================================================

Implemented and confirmed clean:

Playback scope fix:
- useAudioEngine.ts now detects when the arrangement lane is empty.
- If no arrangement clips exist, Play falls back to source-track playback.
- Playback source priority:
  1. selected track
  2. reference / MASTER track
  3. first imported track
- Tick loop auto-stops when source track ends.
- Play/Stop now work before arrangement clips are added.

Snap-to-grid:
- Global snap toggle added.
- Snap defaults ON.
- Snap resolution added:
  - Bar
  - Beat
  - 1/2 beat
  - 1/4 beat

New shared utility:
- src/lib/snapUtils.ts

Added helpers:
- snapPosition(pos, bpm, snapEnabled, resolution, shiftHeld)
- snapForward(pos, bpm, snapEnabled, resolution)

Store additions:
- snapEnabled
- snapResolution
- snapGuidePosition
- related setter actions

UI:
- LeftSidebar now includes SNAP ON/OFF toggle.
- Resolution dropdown added.
- Note added that Shift inverts snap behavior during drag.

Clip drag/trim:
- ClipBlock drag now uses shared snap utilities.
- Move, trim-left, and trim-right all snap through snapPosition.
- Shift key inverts global snap toggle.
- Drag emits snapGuidePosition into store.

Timeline:
- Timeline reads snapGuidePosition.
- Renders subtle cyan vertical snap guide line with glow.
- Guide disappears on pointer-up.
- Append insert positions use snapForward to land on next clean grid boundary.

Validation:
- App restarted cleanly after transient HMR hook-order warning.
- TypeScript check passed with zero errors.

NO PITCH DRIFT rule:
- unchanged and preserved.
- Stretch behavior remains labelled DRAFT ONLY / changes pitch.

====================================================
CURRENT CORE WORKFLOW
====================================================

Current working loop:

1. import tracks
2. detect BPM
3. play source tracks immediately
4. segment tracks
5. append/replace sections
6. snap to grid
7. seam audition
8. adjust fades
9. export WAV

====================================================
CURRENT PRIORITY
====================================================

Do NOT add:
- waveform seam AI
- BPM tap overlays
- AI analysis
- SaaS/cloud systems
- mobile layouts
- unrelated feature creep

Focus now on:
- workflow testing
- seam quality
- export reliability
- editor feel
- real-world song reconstruction